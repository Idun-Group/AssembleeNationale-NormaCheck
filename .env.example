# NormaCheck — variables d'environnement
#
# Aucune variable n'est requise pour le hackathon : l'analyse LLM utilise
# l'authentification locale de la CLI `claude` (pas de clé API).
#
# Si une bascule vers l'API Anthropic est nécessaire plus tard, décommenter :
# ANTHROPIC_API_KEY=
